class Solution {
    public boolean uniformArray(int[] nums1) {
        return true;
    }
}

//Always possible because, if all elements of array is even then ok,
// if all elements of array are odd then also ok
// and now if there present even a single odd or single even then
//we can simply make an array of odd's in that case